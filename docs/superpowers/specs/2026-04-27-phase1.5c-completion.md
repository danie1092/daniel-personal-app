# Phase 1.5c 완료 보고 (2026-04-27)

**브랜치**: `phase1.5c-diary-and-cleanup`
**최종 검증**: `npm test` 98 PASS, `npm run build` **env 없이 성공**.

## 적용된 변경

### 코드 — 신규 (10 파일)
- `src/app/(main)/diary/emotionTokens.ts` — 8 감정 토큰 (emoji/label/bg/dotHex)
- `src/lib/diary/data.ts` (+ test, 5 tests) — getTodayDiary/getRecentDiaries/getMonthGrass
- `src/app/(main)/diary/actions.ts` (+ test, 8 tests) — upsertTodayDiary/updateDiary/deleteDiary
- `src/app/(main)/diary/DiaryGrass.tsx` — 30일 잔디 카드
- `src/app/(main)/diary/DiaryInputCard.tsx` — 감정 chip + textarea + 사운드
- `src/app/(main)/diary/DiaryEditSheet.tsx` — 과거 일기 보기/수정/삭제
- `src/app/(main)/diary/DiaryPastList.tsx`

### 코드 — 재작성
- `src/app/(main)/diary/page.tsx` — 183줄 클라이언트 → 64줄 async Server Component

### 잔여 정리 (이번 phase에 묶음)
- `src/app/api/budget/auto/route.ts` module-level supabase init → `getSupabase()` 함수화 (빌드 시점 env 없이도 통과)
- `src/app/(auth)/login/page.tsx` `dynamic = "force-dynamic"` + `createClient()` 함수 안으로 이동 + `.ios-input` 클래스 → 토큰 클래스
- `src/lib/supabase.ts` 삭제 (consumer 없음)
- `src/app/api/inbox/organize/`, `src/app/api/inbox/save/` 삭제 (Phase 1.5a Server Action으로 흡수)

## 검증 결과

| 항목 | 상태 |
|------|------|
| `npm test` | ✅ 17 files / 98 tests PASS (Phase 1.5a 85 + 신규 13: data 5 + actions 8) |
| `npm run build` (env 없이) | ✅ **성공** (가장 큰 개선) |
| /diary | ✅ dynamic, 새 디자인 |
| 클라이언트 supabase 직접 호출 (전체 main) | ✅ 0건 |
| `src/lib/supabase.ts` | ✅ 삭제됨 |
| `/api/inbox/{organize,save}` | ✅ 삭제됨 |

## 보안 / 검증
- 모든 Server Action: `requireSession` + 입력 검증 (content ≤ 8192, emotion whitelist)
- console.error 메시지만
- `playTypingSound` 외부 의존 없는 Web Audio synth, 비용 0

## Phase 1.5 전체 결산

✅ Phase 1.5b (가계부) → 1.5d (루틴) → 1.5a (메모) → **1.5c (일기)** 모두 완료

| 페이지 | 변경 전 | 변경 후 | 신규 컴포넌트 |
|--------|---------|---------|----------------|
| 가계부 | 755줄 | 60줄 | 13 파일 |
| 루틴 | 547줄 | 92줄 | 12 파일 |
| 메모 | 432줄 | 70줄 | 10 파일 |
| 일기 | 183줄 | 64줄 | 10 파일 |

**총 1,917줄 클라이언트 컴포넌트 → 286줄 async Server Component + 45개 책임별 파일**

전체 Phase 1.5 테스트 누적: Phase 1 42 → Phase 1.5b 56 → 1.5d 70 → 1.5a 85 → 1.5c **98**.

## 알려진 미해결 (Phase 2~3)
- `routine_items.{pokemon_id, level, exp}` DB 컬럼 drop (Phase 2 마이그레이션)
- `/api/budget/auto` endpoint 인증 + 정리 (Phase 3 SMS 자동입력 시점)
- 큐레이션 자동 수집 (Phase 2)
- SMS 가계부 자동입력 (Phase 3)
- public/sw.js gitignore 처리 (매 빌드마다 dirty)

## 커밋 흐름 (8 commits)

```
de21f83 fix(build): /api/budget/auto + /login module-level supabase init 정리
13f75fb feat: emotionTokens
247525d feat(lib/diary): data.ts + 5 tests
d8a3164 feat(diary): actions.ts + 8 tests
49c5cde feat(diary): 컴포넌트 4종
45289c2 feat(diary): page.tsx 재작성 + lib/supabase.ts + /api/inbox/{org,save} 삭제
(+ 이 완료 보고)
```
