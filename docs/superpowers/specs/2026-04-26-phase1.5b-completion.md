# Phase 1.5b 완료 보고 (2026-04-26)

**브랜치**: `phase1.5-pages-renovation`
**최종 검증**: `npm test` 56 PASS, `npm run build` 성공, /budget dynamic 라우트로 전환.

## 적용된 변경

### 코드 — 신규 (13 파일)
- `src/lib/budget/categoryTokens.ts` — 13 카테고리 emoji/bg/text/hex 매핑
- `src/lib/budget/monthData.ts` (+ test, 4 tests) — 월별 데이터 헬퍼
- `src/app/(main)/budget/actions.ts` (+ test, 10 tests) — Server Actions
- `src/app/(main)/budget/DonutChart.tsx`
- `src/app/(main)/budget/BudgetMonthSelector.tsx`
- `src/app/(main)/budget/BudgetTabs.tsx`
- `src/app/(main)/budget/EntryRow.tsx`
- `src/app/(main)/budget/EntryEditSheet.tsx`
- `src/app/(main)/budget/DetailsFilter.tsx`
- `src/app/(main)/budget/DetailsTab.tsx`
- `src/app/(main)/budget/InputTab.tsx`
- `src/app/(main)/budget/SummaryTab.tsx`
- `src/app/(main)/budget/FixedExpenseButton.tsx`

### 코드 — 재작성
- `src/app/(main)/budget/page.tsx` — 755줄 클라이언트 컴포넌트 → 60줄 async Server Component

### 데이터베이스
변경 없음.

## 검증 결과

| 항목 | 상태 |
|------|------|
| `npm test` | ✅ 11 files / 56 tests PASS (Phase 1 42 + Phase 1.5b 14) |
| `npm run build` | ✅ 성공, /budget dynamic (`ƒ`) |
| 클라이언트 supabase 직접 호출 (budget 디렉토리) | ✅ 0건 |
| 신규 코드 lint | ✅ clean |

## 보안

모든 신규 Server Action에 다음이 적용됨:
- `requireSession()` 호출
- 입력 검증: amount(0..999_999_999, 정수), description(≤200), memo(≤500), date(YYYY-MM-DD), category(BUDGET_CATEGORIES 화이트리스트), paymentMethod(PAYMENT_METHODS 화이트리스트 또는 null)
- 월급/저축 카테고리 → paymentMethod 자동 null
- error 로깅 sanitize (raw error 객체 X)
- service role key 사용 X (모두 SSR client)

## 알려진 미해결 (Phase 2~3 이연)

| 항목 | Phase |
|------|-------|
| `/api/budget/auto` endpoint 무인증 + module-level supabase init | 3 (SMS 자동입력 시 정리) |
| 가계부 자동 카테고리 분류 (가맹점명 → 카테고리) | 3 |
| 예산 알림 푸시 | 향후 |
| 사용자 설정 (월별 예산 변경) | 향후 |

## 작업 중 발견된 이슈

- **Task 3 type bug**: `ActionResult<T = void>`의 default `void`가 `{ok:true} & void = never`로 collapse되어 type error 유발. Tasks 4+5 수행 중 발견되어 default를 `object`로 수정 (커밋 `e93f900`).

## 커밋 흐름 (13 commits)

```
4d1a3e1 docs: design doc
b50d527 docs: implementation plan
3450032 feat: categoryTokens
7c9a811 feat: monthData (+ 4 tests)
568b6f8 feat: actions.ts (+ 10 tests)
e93f900 fix: ActionResult void→object (Task 3 후속)
bfa52a6 feat: DonutChart
1d421fb feat: MonthSelector + Tabs
1a617e0 feat: EntryRow + EntryEditSheet + DetailsFilter
a6987da feat: DetailsTab
579cbf1 feat: InputTab
83a8f89 feat: SummaryTab + FixedExpenseButton
ffcb302 feat: page.tsx 재작성
```
